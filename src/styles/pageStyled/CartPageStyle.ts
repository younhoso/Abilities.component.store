'use client';

import styled from 'styled-components';

export const CartPageStyle = styled.div`
  .inner {
    display: flex;
    > p {
      flex: 0.1;
    }
    .box {
      border: 1px solid;
    }
  }
`;
