'use client';

import styled from 'styled-components';

export const NotificationTriggerStyled = styled.div`
  .confirMmodal {
    position: absolute;
    z-index: 999;
    background-color: #fff;
    top: 50%;
    left: 50%;
    -webkit-transform: translate(-50%, -50%);
    transform: translate(-50%, -50%);
  }
`;
