'use client';

import styled from 'styled-components';

interface TransitionProps {}

export const TransitionStyled = styled.div<TransitionProps>``;

export const TransitionItemStyled = styled.div<TransitionProps>``;
