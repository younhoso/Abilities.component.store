'use client';

import styled from 'styled-components';

export const ToggleGroupStyled = styled.div``;

export const ToggleGroupItemStyled = styled.div`
  display: inline-block;
  padding: 0 10px;
  &.off {
    background-color: #fff;
  }
  &.on {
    background-color: blue;
  }
`;
