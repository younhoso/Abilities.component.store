'use client';

import styled from 'styled-components';

export const ScrollElementsStyled = styled.div`
  .card {
    margin: 20px 0;
    padding: 20px;
    border: 1px solid #ccc;
    border-radius: 8px;
    transition: transform 0.3s ease-in-out;
  }
`;
